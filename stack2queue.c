#include"QUEUELLADT.h"
queue *q1,*q2;
q1=creatqueue();
q2=creatqueue();
int main()
{
  int x,ch;
  while(1)
  {
    printf("\nenter the choice\n1)push\n2)pop\n3)display\n");
    scanf("%d",&ch);
    switch(ch)
    {
        case 1:printf("ener the number\n");
               scanf("%d",&x);
               push()
    }
  }
}